script: ./.travis/build
before_install:
- git submodule update --init --recursive
compiler:
- clang
- gcc
notifications:
  email:
  - francois.keith@gmail.com
branches:
  only:
  - master
  - robohow_2
language: cpp
matrix:
  allow_failures:
  - compiler: clang
